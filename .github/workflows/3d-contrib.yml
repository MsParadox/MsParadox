name: 3D Contrib
on:
  schedule: [{ cron: "0 18 * * *" }]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: MsParadox
          SETTING_JSON: |
            {"username":"MsParadox","type":"night-rainbow"}
      - run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A && git diff-index --quiet HEAD || git commit -m "Update 3D contrib" && git push
